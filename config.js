// For storing all the configuration files here
const config = {
    MONGO_URL: "mongodb://localhost/aston",
    SALT_ROUNDS: 3 // Increase number to increase pass leak security
};

module.exports = config